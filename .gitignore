node_modules
.next
out
dist
.env
.env.local
*.log
.DS_Store
next-env.d.ts
*.tsbuildinfo
.vercel
coverage
item.json

# Local-only: demo-video production assets + internal runtime-decision notes.
# The generator script (scripts/) is tracked; its rendered output is not.
docs/
ARCHITECTURE_DECISION.md

# SQLite persistence (local data, not for git)
data/
